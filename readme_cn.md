# WindFetch 🌀

[English](https://github.com/starwindv/windfetch/blob/main/readme.md)

优雅的 Python 系统信息获取工具, 灵感来自 `neofetch`

## ✨ 特性

### 🎯 核心功能
- **系统信息展示** - 全面的系统硬件和软件信息
- **Logo 自动匹配** - 根据系统名称自动显示对应的 ASCII Logo 并对齐输出格式
- **彩色输出** - 美观的终端色彩支持
- **智能检测** - 自动识别桌面环境、主题、图标等
- **高度可定制** - 支持自定义 Logo 和配置

### 🎨 Logo 系统

#### 默认 Logo 示例
项目内置链接提供了多个常见系统的 ASCII Logo: 
- **Red Hat** - 经典红帽 Logo
- **Ubuntu** - Ubuntu 标志性 Logo
- **Windows** - Windows 彩色 Logo (仅Logo支持, 系统检测暂不支持) 
- **Debian** - Debian 旋涡 Logo
- **CentOS** - CentOS 几何 Logo
- **Arch Linux** - Arch 标志性 Logo
- **Android** - Android 机器人 Logo

#### 自定义 Logo
你可以为任何系统创建自定义 Logo: 

**方法一: JSON 格式 (推荐) **
```json
{
    "mysystem": "你的 ASCII 艺术文字",
    "ubuntu": "\u001b[38;5;123m自定义 Ubuntu Logo\u001b[0m"
}
```

**方法二: Python 格式 (需启用不安全加载) **
```python
logo = {
    "mysystem": "彩色 ASCII 艺术"
}
```

### ⚙️ 配置管理
- 在线 Logo 下载与更新
- 自定义 Logo 支持
- 安全加载控制
- 灵活的配置管理

## 🚀 快速开始

### 安装

```bash
pip install windfetch
```

### 基本使用

```bash
windfetch
```

## 📋 显示信息

WindFetch 会显示以下系统信息: 

| 类别       | 信息内容            |
|----------|-----------------|
| **用户信息** | 用户名@主机名         |
| **系统信息** | 操作系统、内核版本、运行时间  |
| **软件信息** | 包数量、Shell、终端    |
| **桌面环境** | DE、主题、图标主题      |
| **硬件信息** | CPU、GPU、内存使用情况等 |

## ⚠️ 平台支持说明

**目前仅支持 Linux 系统**
- ✅ 完整支持常见 Linux 发行版
- ❌ Windows 系统检测暂未适配
- ❌ macOS 系统检测暂未适配

在不受支持的平台上, 部分系统信息可能显示为 "Unknown"。

## ⚙️ 配置

### 配置文件位置
`~/.windfetch/config.json`

### 默认配置

```json
{
    "logo_path": "~/.windfetch/logo.py",
    "custom_logo_path": null,
    "logo_url": "https://windfetch.starwindv.top/api/logo",
    "download_default_logo": true,
    "overwrite_default_logo": true,
    "load_customLogo_unsafe": false,
    "need_update_logo": false
}
```

### 配置说明

- **`logo_path`** - 默认 Logo 文件路径 (不建议手动修改) 
- **`custom_logo_path`** - 自定义 Logo 文件路径
- **`logo_url`** - Logo 数据源 URL
- **`download_default_logo`** - 是否自动下载默认 Logo
- **`load_customLogo_unsafe`** - 是否允许加载 Python 格式的自定义 Logo

## 🔧 自定义 Logo 指南

### 安全建议
1. **优先使用 JSON 格式** - 更安全, 无需代码执行
2. **谨慎使用 Python 格式** - 仅在信任的源中使用
3. **不要修改默认 logo.py** - 工具会自动更新, 手动修改可能被覆盖

### 创建自定义 Logo

1. **创建 JSON 或 .py 格式 Logo 文件**

2. **更新配置文件**
```json
{
    "custom_logo_path": "/path/to/your/logo.json",
    "load_customLogo_unsafe": true
}
```
注意, 不论是否使用 .py 格式的自定义 logo, `load_customLogo_unsafe`都需要为`true`才能正确加载它

## 🛠️ 开发

### 项目结构
```
.
├── ExampleLogos/
│   ├── logo.json
│   └── logo.py
├── pyproject.toml
└── src/
    └── windfetch/
        ├── __init__.py
        ├── loader.py
        └── windfetch.py
```

### 依赖项
- `requests` - HTTP 请求处理
- `stv_utils>=0.0.8` - 工具函数和彩色输出
- `psutil` - 系统信息获取

## 安全提醒
- 启用 `load_customLogo_unsafe` 时请确保自定义 Logo 文件来源可信
- 定期检查默认 Logo 文件是否被意外修改

## 📄 许可证

本项目采用 [MIT 许可证](https://github.com/starwindv/windfetch) 

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📞 联系

- 作者: 星灿长风 (StarWindv)
- 邮箱: starwindv.stv@gmail.com
- 项目主页: https://github.com/StarWindv/windfetch
